# Preset training configuration for SmolLM2-1.7B-Instruct (FFT).
# Based on SmolLM2-135M-Instruct config with adjustments for larger model.

model:
  model_name: "HuggingFaceTB/SmolLM2-1.7B-Instruct"
  torch_dtype_str: "bfloat16"
  attn_implementation: "sdpa"
  trust_remote_code: False
  model_max_length: 8192

data:
  train:
    collator_name: "text_completions_only_with_padding"
    collator_kwargs:
      response_template: "<|im_start|>assistant\n"
      instruction_template: "<|im_start|>user\n"
    seed: 666
    datasets:
      - dataset_name: text_sft
        dataset_path: OVERRIDE_ME  # Override with --data.train.datasets.0.dataset_path
        dataset_kwargs:
          return_conversations: true
          return_conversations_format: dict

  validation:
    collator_name: "text_completions_only_with_padding"
    collator_kwargs:
      response_template: "<|im_start|>assistant\n"
      instruction_template: "<|im_start|>user\n"
    seed: 666
    datasets:
      - dataset_name: text_sft
        dataset_path: OVERRIDE_ME  # Override with --data.validation.datasets.0.dataset_path
        dataset_kwargs:
          return_conversations: true
          return_conversations_format: dict

training:
  trainer_type: "TRL_SFT"
  output_dir: "output"
  run_name: "placeholder"
  save_final_model: true
  save_steps: 0
  save_epoch: false
  max_steps: -1
  num_train_epochs: 2
  # Effective batch size on 8-gpu node: 2 * 2 * 8 = 32
  per_device_train_batch_size: 2
  gradient_accumulation_steps: 2

  enable_gradient_checkpointing: true
  gradient_checkpointing_kwargs:
    use_reentrant: false
  ddp_find_unused_parameters: false
  optimizer: "adamw_torch_fused"
  learning_rate: 7.0e-05
  lr_scheduler_type: "cosine"
  warmup_ratio: 0.05
  weight_decay: 0.05
  compile: false

  dataloader_num_workers: "auto"
  dataloader_prefetch_factor: 32

  seed: 666
  use_deterministic: true

  logging_steps: 5
  eval_strategy: "steps"
  eval_steps: 10  # Run validation every 10 steps
  log_model_summary: false
  empty_device_cache_steps: 50
  include_performance_metrics: true
  enable_wandb: true

fsdp:
  enable_fsdp: true
  sharding_strategy: "HYBRID_SHARD"
  forward_prefetch: true
  auto_wrap_policy: "TRANSFORMER_BASED_WRAP"
  transformer_layer_cls: "LlamaDecoderLayer"  # SmolLM2 uses Llama architecture
