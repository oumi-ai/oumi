# Preset training configuration for Qwen3-8B (FFT).
# Based on Qwen3-4B config with adjustments for larger model.

model:
  model_name: "Qwen/Qwen3-8B"
  torch_dtype_str: "bfloat16"
  attn_implementation: "sdpa"
  trust_remote_code: false
  model_max_length: 16384

data:
  train:
    collator_name: "text_completions_only_with_padding"
    collator_kwargs:
      response_template: "<|im_start|>assistant\n"
      instruction_template: "<|im_start|>user\n"
    seed: 42
    datasets:
      - dataset_name: "text_sft"
        dataset_path: "REQUIRED_VIA_SCRIPT"  # Overridden by --data.train.datasets.0.dataset_path
        dataset_kwargs:
          return_conversations: true
          return_conversations_format: "dict"

  validation:
    collator_name: "text_completions_only_with_padding"
    collator_kwargs:
      response_template: "<|im_start|>assistant\n"
      instruction_template: "<|im_start|>user\n"
    seed: 42
    datasets:
      - dataset_name: "text_sft"
        dataset_path: "REQUIRED_VIA_SCRIPT"  # Overridden by --data.validation.datasets.0.dataset_path
        dataset_kwargs:
          return_conversations: true
          return_conversations_format: "dict"

training:
  trainer_type: "TRL_SFT"
  output_dir: "REQUIRED_VIA_SCRIPT"  # Overridden by --training.output_dir
  run_name: "REQUIRED_VIA_SCRIPT"  # Overridden by --training.run_name
  save_final_model: true
  save_steps: 0
  save_epoch: false
  max_steps: -1
  num_train_epochs: 3
  # Effective batch size on 8-gpu node: 1 * 8 * 8 = 64
  per_device_train_batch_size: 1
  per_device_eval_batch_size: 1
  gradient_accumulation_steps: 8
  max_grad_norm: 1.0

  enable_gradient_checkpointing: true
  gradient_checkpointing_kwargs:
    use_reentrant: false
  ddp_find_unused_parameters: false
  optimizer: "adamw_torch_fused"
  learning_rate: 2.0e-05
  lr_scheduler_type: "cosine"
  warmup_ratio: 0.1
  weight_decay: 0.01
  compile: false

  dataloader_num_workers: "auto"
  dataloader_prefetch_factor: 8

  logging_steps: 5
  eval_strategy: "steps"
  eval_steps: 10  # Run validation every 10 steps
  log_model_summary: false
  empty_device_cache_steps: 50
  include_performance_metrics: true
  enable_wandb: true

fsdp:
  enable_fsdp: true
  sharding_strategy: "HYBRID_SHARD"
  forward_prefetch: true
  auto_wrap_policy: "TRANSFORMER_BASED_WRAP"
  transformer_layer_cls: "Qwen3DecoderLayer"
