# Preset training configuration for Qwen3-0.6B (FFT).

model:
  model_name: "Qwen/Qwen3-0.6B"
  torch_dtype_str: "bfloat16"
  attn_implementation: "sdpa"
  trust_remote_code: false
  model_max_length: 8192

data:
  train:
    collator_name: "text_completions_only_with_padding"
    collator_kwargs:
      response_template: "<|im_start|>assistant\n"
      instruction_template: "<|im_start|>user\n"
    seed: 666
    datasets:
      - dataset_name: "text_sft"
        dataset_path: "REQUIRED_VIA_SCRIPT"
        dataset_kwargs:
          return_conversations: true
          return_conversations_format: "dict"

  validation:
    collator_name: "text_completions_only_with_padding"
    collator_kwargs:
      response_template: "<|im_start|>assistant\n"
      instruction_template: "<|im_start|>user\n"
    seed: 666
    datasets:
      - dataset_name: "text_sft"
        dataset_path: "REQUIRED_VIA_SCRIPT"
        dataset_kwargs:
          return_conversations: true
          return_conversations_format: "dict"

training:
  trainer_type: "TRL_SFT"
  output_dir: "REQUIRED_VIA_SCRIPT"
  run_name: "REQUIRED_VIA_SCRIPT"
  save_final_model: true
  save_steps: 0
  save_epoch: false
  max_steps: -1
  num_train_epochs: 2
  # Effective batch size on 8-gpu node: 4 * 2 * 8 = 64
  per_device_train_batch_size: 4
  gradient_accumulation_steps: 2
  max_grad_norm: 1.0

  enable_gradient_checkpointing: true
  gradient_checkpointing_kwargs:
    use_reentrant: false
  ddp_find_unused_parameters: false
  optimizer: "adamw_torch_fused"
  learning_rate: 2.0e-05
  lr_scheduler_type: "cosine"
  warmup_ratio: 0.05
  weight_decay: 0.01
  compile: false

  dataloader_num_workers: "auto"
  dataloader_prefetch_factor: 32

  logging_steps: 5
  eval_strategy: "steps"
  eval_steps: 10
  log_model_summary: false
  empty_device_cache_steps: 50
  include_performance_metrics: true
  enable_wandb: true
