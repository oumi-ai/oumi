# Job config to eval gpt-oss-20b.
#
# Requirements:
#   - Set up SkyPilot Lambda: https://oumi.ai/docs/en/latest/user_guides/launch/launch.html#setup
#   - Log into WandB (`wandb login`) or disable `enable_wandb`
#
# Usage:
#   oumi launch up -c oumi://configs/recipes/gpt_oss/evaluation/20b_lambda_job.yaml --cluster gpt-oss-20b-eval
#
# See Also:
#   - Documentation: https://oumi.ai/docs/en/latest/user_guides/launch/launch.html
#   - Config class: oumi.core.configs.JobConfig
#   - Config source: https://github.com/oumi-ai/oumi/blob/main/src/oumi/core/configs/job_config.py
#   - Other job configs: configs/**/*job.yaml

name: gpt-oss-20b-eval

resources:
  cloud: lambda
  accelerators: "H100:1"
  use_spot: false
  disk_size: 400 # Disk size in GBs

working_dir: .

file_mounts:
  ~/.netrc: ~/.netrc # WandB credentials

envs:
  # NOTE: For SFT, update this to point to your model checkpoint.
  # NOTE: For LoRA, instead update this to point to your LoRA adapter.
  #       The base model will be inferred automatically.
  MODEL_CHECKPOINT_DIR: openai/gpt-oss-20b
  WANDB_PROJECT: oumi-eval
  OUMI_RUN_NAME: gpt-oss-20b.eval

setup: |
  set -e
  pip install uv && uv pip install oumi[gpu] hf_transfer
  uv pip install --upgrade accelerate transformers kernels
  uv pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/test/cu128
  uv pip install git+https://github.com/triton-lang/triton.git@main#subdirectory=python/triton_kernels

run: |
  set -e  # Exit if any command failed.
  source ./configs/examples/misc/sky_init.sh

  if test ${OUMI_NUM_NODES} -ne 1; then
    echo "LM Harness supports max 1 node. Actual: ${OUMI_NUM_NODES} nodes."
    exit 1
  fi

  echo "Starting evaluation for ${MODEL_CHECKPOINT_DIR} ..."
  set -x

  oumi evaluate \
    -c oumi://configs/recipes/gpt_oss/evaluation/20b_eval.yaml \
    --run_name "${OUMI_RUN_NAME}.${SKYPILOT_TASK_ID}" \
    --model.model_name "${MODEL_CHECKPOINT_DIR}"

  echo "Node ${SKYPILOT_NODE_RANK} is all done!"
