# ⚠️  COMPLEX: vLLM Inference config for Qwen3-235B-A22B-Thinking GGUF (LIKELY MULTI-PART)
#
# ⚠️  WARNING: This model likely uses MULTI-PART GGUF files due to its size (235B parameters)
# Large models often require sharded GGUF files that need special handling
#
# Usage (ADVANCED - May require multi-part download):
#   # 1. Check if model has single files or multiple parts:
#   huggingface-cli scan-cache unsloth/Qwen3-235B-A22B-Thinking-2507-GGUF
#
#   # 2. For single files:
#   huggingface-cli download unsloth/Qwen3-235B-A22B-Thinking-2507-GGUF \
#     Qwen3-235B-A22B-Thinking-2507-Q4_K_M.gguf --local-dir .
#
#   # 3. For multi-part files (if needed):
#   mkdir -p models/qwen3_235b_thinking_q4km
#   huggingface-cli download unsloth/Qwen3-235B-A22B-Thinking-2507-GGUF \
#     --include "Q4_K_M/*" --local-dir models/qwen3_235b_thinking_q4km/
#
#   # 4. Run inference:
#   oumi infer -i -c configs/recipes/qwen3/inference/235b_a22b_thinking_gguf_infer.yaml
#
# Model Info:
#   - Repository: unsloth/Qwen3-235B-A22B-Thinking-2507-GGUF
#   - Size: ~235B parameters (quantized)
#   - Available quantizations: Q2_K, Q3_K_M, Q4_K_M, Q5_K_M, Q6_K, Q8_0, F16
#   - Requirements: Very high VRAM (recommend 8x A100-80GB or equivalent), Linux/Windows with GPU (macOS has Triton issues)
#   - Special: Thinking/reasoning model with chain-of-thought capabilities
#   - Complexity: HIGH - May require multi-part file handling
#
# See Also:
#   - Documentation: https://oumi.ai/docs/en/latest/user_guides/infer/infer.html
#   - Config class: oumi.core.configs.InferenceConfig
#   - Config source: https://github.com/oumi-ai/oumi/blob/main/src/oumi/core/configs/inference_config.py
#   - Other inference configs: configs/**/inference/

model:
  # GGUF filename (must be in current working directory)
  # May require multi-part handling for this large model
  # Single file: "Qwen3-235B-A22B-Thinking-2507-Q4_K_M.gguf"
  # Multi-part: "models/qwen3_235b_thinking_q4km/Q4_K_M/" (if sharded)
  model_name: "Qwen3-235B-A22B-Thinking-2507-Q4_K_M.gguf"
  # Original model tokenizer for proper text processing
  tokenizer_name: "Qwen/Qwen3-235B-A22B-Thinking"
  model_max_length: 8192
  torch_dtype_str: "float16"  # GGUF models require float16
  trust_remote_code: True
  attn_implementation: "sdpa"

generation:
  max_new_tokens: 4096  # Larger for thinking/reasoning responses
  temperature: 0.7
  top_p: 0.9

engine: VLLM

# Optional: vLLM specific parameters
# vllm_args:
#   tensor_parallel_size: 8  # Adjust based on available GPUs
#   gpu_memory_utilization: 0.90
#   max_num_seqs: 8  # Lower for large model

# Available quantization files (update model_name accordingly):
# "Qwen3-235B-A22B-Thinking-2507-Q2_K.gguf"    # Lower quality, less VRAM
# "Qwen3-235B-A22B-Thinking-2507-Q3_K_M.gguf"  # Good quality
# "Qwen3-235B-A22B-Thinking-2507-Q4_K_M.gguf"  # Balanced (recommended)
# "Qwen3-235B-A22B-Thinking-2507-Q5_K_M.gguf"  # Better quality
# "Qwen3-235B-A22B-Thinking-2507-Q6_K.gguf"    # High quality
# "Qwen3-235B-A22B-Thinking-2507-Q8_0.gguf"    # Very high quality
#
# NOTE: Due to model size (235B), files may be sharded into multiple parts
