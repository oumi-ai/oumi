# Phi3 LoRA train config for macOS.
#
# Usage:
#   oumi train -c configs/recipes/phi3/sft/lora_macos_train.yaml
#
# See Also:
#   - Documentation: https://oumi.ai/docs/en/latest/user_guides/train/train.html
#   - Config class: oumi.core.configs.TrainingConfig
#   - Config source: https://github.com/oumi-ai/oumi/blob/main/src/oumi/core/configs/training_config.py
#   - Other training configs: configs/**/*train.yaml

model:
  model_name: "microsoft/Phi-3-mini-4k-instruct"
  tokenizer_pad_token: "<unk>"  # Avoid pad==eos; use <unk> as padding token per Microsoft's official fine-tuning guide:
  # https://huggingface.co/microsoft/Phi-3-mini-4k-instruct/blob/main/sample_finetune.py
  torch_dtype_str: "float16"

data:
  train:
    datasets:
      - dataset_name: "yahma/alpaca-cleaned"

training:
  optimizer: "adamw_torch"
  use_peft: true
  trainer_type: "TRL_SFT"
  output_dir: "output/phi3.lora"

peft:
  q_lora: False
  lora_target_modules:
    - "qkv_proj"
    - "o_proj"
    - "gate_up_proj"
    - "down_proj"
