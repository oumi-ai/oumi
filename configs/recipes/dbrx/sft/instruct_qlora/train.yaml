# QLoRA config for DBRX-Instruct (132B MoE model with ~36B activated params).
#
# Hardware Requirements:
#   - 8x H100-80GB or equivalent
#   - Estimated ~35GB VRAM per GPU with FSDP + 4-bit
#
# Known Issues:
#   - DBRX has fused expert parameters that don't work well with standard LoRA/QLoRA
#   - 4-bit quantization may have issues with fused experts
#   - See: https://huggingface.co/databricks/dbrx-instruct/discussions/10
#   - Consider using SinclairSchneider/dbrx-instruct-quantization-fixed if issues arise
#
# Requirements:
#   - Log into WandB (`wandb login`) or disable `enable_wandb`
#
# Usage:
#   oumi distributed torchrun -m oumi train -c configs/recipes/dbrx/sft/instruct_qlora/train.yaml
#
# MoE Best Practices:
#   - Only target attention layers (Wqkv, out_proj)
#   - DO NOT target expert layers - DBRX uses fused experts incompatible with PEFT
#
# See Also:
#   - Documentation: https://oumi.ai/docs/en/latest/user_guides/train/train.html
#   - DBRX Model Card: https://huggingface.co/databricks/dbrx-instruct

model:
  model_name: "databricks/dbrx-instruct"
  model_max_length: 32768
  torch_dtype_str: "bfloat16"
  attn_implementation: "flash_attention_2"
  trust_remote_code: True

data:
  train:
    datasets:
      - dataset_name: "yahma/alpaca-cleaned"

training:
  trainer_type: "TRL_SFT"
  use_peft: True
  save_steps: 200
  num_train_epochs: 1
  per_device_train_batch_size: 2
  gradient_accumulation_steps: 8
  max_grad_norm: 1.0

  enable_gradient_checkpointing: True
  gradient_checkpointing_kwargs:
    use_reentrant: False
  ddp_find_unused_parameters: False
  optimizer: "adamw_torch_fused"
  learning_rate: 2.0e-04
  lr_scheduler_type: "cosine"
  warmup_steps: 100
  weight_decay: 0.01
  compile: False

  dataloader_num_workers: "auto"
  dataloader_prefetch_factor: 16

  logging_steps: 10
  empty_device_cache_steps: 25
  output_dir: "output/dbrx_instruct.qlora"
  include_performance_metrics: True
  enable_wandb: True

peft:
  q_lora: True
  bnb_4bit_quant_type: "nf4"
  bnb_4bit_quant_storage: "bfloat16"
  bnb_4bit_compute_dtype: "bfloat16"
  lora_r: 16
  lora_alpha: 32
  # DBRX uses fused attention (Wqkv) - only target attention layers
  lora_target_modules:
    - "Wqkv"
    - "out_proj"

fsdp:
  enable_fsdp: True
  forward_prefetch: True
  sharding_strategy: "FULL_SHARD"
  auto_wrap_policy: "TRANSFORMER_BASED_WRAP"
  transformer_layer_cls: "DbrxBlock"
