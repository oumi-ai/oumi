# Groundedness Judge Template
# Used for evaluating factual accuracy

system_instruction: |
  You are an expert at evaluating factual accuracy.
  Assess whether claims in the response are supported by the provided context.

prompt_template: |
  Context: {context}
  Response: {response}

  Is the response grounded in and supported by the context?

judgment_type: BOOL
