name: lema-train-example

resources:
  # Use 1 of the following GPUs depending on availability. No preference.
  # To view other GPU types use the following commands:
  # `sky show-gpus`, `sky show-gpus -a`
  accelerators: {"A100"}  # "A100-80GB-SXM", "H100-80GB-SXM", "A100-80GB", "H100-80GB"
  # To configure single-node, multi-gpu (N GPUs) training, set `accelerators:` above to
  # something like this: {"A40": N}
  any_of:
    - use_spot: true
    - use_spot: false

# Upload a working directory to remote ~/sky_workdir.
workdir: .

# Upload local files.
file_mounts:
  ~/.netrc: ~/.netrc  # mount local netrc file to access private repos
  # /artifacts:
  #   name: lema-dev-private # Not available on lambda
  #   mode: MOUNT

envs:
  WANDB_PROJECT: lema-train-test
  LEMA_RUN_NAME: llama2b.pt

setup: |
  set -e
  pip install '.[train]'
  # FLASH_ATTENTION_SKIP_CUDA_BUILD=TRUE pip install flash-attn --no-build-isolation
  pip install flash-attn --no-build-isolation

run: |
  set -e  # Exit if any command failed.

  # Run some checks, and export "LEMA_*" env vars
  source ./configs/skypilot/sky_init.sh

  TOTAL_NUM_GPUS=$((${LEMA_NUM_NODES} * ${SKYPILOT_NUM_GPUS_PER_NODE}))
  ACCELERATE_MULTI_GPU_OPTION=
  if [${TOTAL_NUM_GPUS} -gt 1]; then
    ACCELERATE_MULTI_GPU_OPTION="--multi_gpu"
  fi

  set -x  # Print "accelerate launch" command with expanded variables
  accelerate launch \
      --num_machines ${LEMA_NUM_NODES} \
      --machine_rank ${SKYPILOT_NODE_RANK} \
      --num_processes ${TOTAL_NUM_GPUS} \
      --main_process_ip ${LEMA_MASTER_ADDR} \
      --main_process_port 8007 \
      ${ACCELERATE_MULTI_GPU_OPTION} \
      --config_file configs/accelerate/llama.ddp.yaml \
      -m lema.train \
      -c configs/lema/llama2b.pt.yaml \
      "data.train.experimental_use_async_dataset=true" \
      "training.run_name='${LEMA_RUN_NAME}.${SKYPILOT_TASK_ID}'" \
      "model.compile=false" \
      "training.compile=false" \
      "training.max_steps=20" \
      "training.save_steps=0" \
      "training.save_final_model=false" \
      "training.optimizer='adafactor'" \
      "training.per_device_train_batch_size=14" \
      "training.enable_gradient_checkpointing=true" \
      "training.gradient_accumulation_steps=19" \
      "training.dataloader_num_workers=2" \
      "training.dataloader_prefetch_factor=4" \
      "training.log_model_summary=false" \
      "training.include_performance_metrics=true" \
      "training.ddp_find_unused_parameters=false" \
      "training.try_resume_from_last_checkpoint=false" \
      "training.enable_wandb=true"

  echo "Node ${SKYPILOT_NODE_RANK} is all done!"


# Default configuation
# 1 node, 1 GPUs:
# sky launch -i 10  --env LEMA_RUN_NAME=llama2b.pt.1node.1xA10040GB.nocompile.v106 --cloud GCP --use-spot --num-nodes 1 --gpus "A100:1" --cluster xrdaukar-1node1gpu-01-lema-cluster ./configs/skypilot/sky_llama2b_ddp.yaml

# 1 node, 2 GPUs:
# sky launch -i 10  --env LEMA_RUN_NAME=llama2b.pt.1node.2xA10040GB.nocompile.v105 --cloud GCP --use-spot --num-nodes 1 --gpus "A100:2" --cluster xrdaukar-1node2gpu-01-lema-cluster ./configs/skypilot/sky_llama2b_ddp.yaml
#

# 1 node, 4 GPUs:
# sky launch -i 10  --env LEMA_RUN_NAME=llama2b.pt.4xA10040GB.nocompile.v100 --cloud GCP --use-spot --num-nodes 1 --gpus "A100:4" --cluster xrdaukar-1node4gpu-01-lema-cluster ./configs/skypilot/sky_llama2b_ddp.yaml

# sky launch -i 10  --env LEMA_RUN_NAME=llama2b.pt.4xA10040GB.modelcompile.v102 --cloud GCP --use-spot --num-nodes 1 --gpus "A100:4" --cluster xrdaukar-1node4gpu-02-lema-cluster ./configs/skypilot/sky_llama2b_ddp.yaml

# 1 node, 8 GPUs:
# sky launch -i 10  --env LEMA_RUN_NAME=llama2b.pt.8xA10040GB.nocompile.v100 --cloud GCP --use-spot --num-nodes 1 --gpus "A100:8" --cluster xrdaukar-1node8gpu-01-lema-cluster ./configs/skypilot/sky_llama2b_ddp.yaml
# torchrun:
# sky launch -i 10  --env LEMA_RUN_NAME=llama2b.tr.8xA10040GB.nocompile.v100 --cloud GCP --use-spot --num-nodes 1 --gpus "A100:8" --cluster xrdaukar-1node8gpu-01-lema-cluster ./configs/skypilot/sky_llama2b.yaml


# 4 nodes, 4 GPUs:
# sky launch -i 10  --env LEMA_RUN_NAME=llama2b.pt.4nodes.4xA10040GB.nocompile.v100 --cloud GCP --use-spot --num-nodes 4 --gpus "A100:4" --cluster xrdaukar-4node4gpu-01-lema-cluster ./configs/skypilot/sky_llama2b_ddp.yaml

# 8 nodes, 4 GPUs:
# sky launch -i 10  --env LEMA_RUN_NAME=llama2b.pt.8nodes.4xA10040GB.nocompile.v100 --cloud GCP --use-spot --num-nodes 8 --gpus "A100:4" --cluster xrdaukar-8node4gpu-01-lema-cluster ./configs/skypilot/sky_llama2b_ddp.yaml
