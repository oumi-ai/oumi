name: lema-train-example

resources:
  # Use 1 of the following GPUs depending on availability. No preference.
  # To view other GPU types use the following commands:
  # `sky show-gpus`, `sky show-gpus -a`
  accelerators: {"A100": 4}
  # To configure single-node, multi-gpu (N GPUs) training, set `accelerators:` above to
  # something like this: {"A40": N}
  any_of:
    - use_spot: true
    - use_spot: false

# Upload a working directory to remote ~/sky_workdir.
workdir: .

# Upload local files.
file_mounts:
  ~/.netrc: ~/.netrc  # mount local netrc file to access private repos
  # Mount HF token, which is needed to download locked-down models from HF Hub.
  # This is created on the local machine by running `huggingface-cli login`.
  ~/.cache/huggingface/token: ~/.cache/huggingface/token
  # /artifacts:
  #   name: lema-dev-private # Not available on lambda
  #   mode: MOUNT
  /output_dir_gcs:
    source: gs://lema-dev-europe-west4/
    store: gcs
    mode: MOUNT

envs:
  WANDB_PROJECT: lema-train-test
  LEMA_RUN_NAME: llama8b.lora
  ACCELERATE_LOG_LEVEL: info
  # https://github.com/huggingface/tokenizers/issues/899#issuecomment-1027739758
  TOKENIZERS_PARALLELISM: false

setup: |
  set -e
  pip install '.[train]'
  # FLASH_ATTENTION_SKIP_CUDA_BUILD=TRUE pip install flash-attn --no-build-isolation
  pip install flash-attn --no-build-isolation
  # Install model from HF Hub. This tool increases download speed compared to
  # downloading the model during training.
  pip install huggingface_hub[hf_transfer]
  HF_HUB_ENABLE_HF_TRANSFER=1 huggingface-cli download meta-llama/Meta-Llama-3.1-8B

run: |
  set -e  # Exit if any command failed.

  # Run some checks, and export "LEMA_*" env vars
  source ./configs/skypilot/sky_init.sh

  TOTAL_NUM_GPUS=$((${LEMA_NUM_NODES} * ${SKYPILOT_NUM_GPUS_PER_NODE}))
  ACCELERATE_MULTI_GPU_OPTION=
  if [${TOTAL_NUM_GPUS} -gt 1]; then
    ACCELERATE_MULTI_GPU_OPTION="--multi_gpu"
  fi

  set -x  # Print "accelerate launch" command with expanded variables
  accelerate launch \
      --num_machines ${LEMA_NUM_NODES} \
      --machine_rank ${SKYPILOT_NODE_RANK} \
      --num_processes ${TOTAL_NUM_GPUS} \
      --main_process_ip ${LEMA_MASTER_ADDR} \
      --main_process_port 8007 \
      ${ACCELERATE_MULTI_GPU_OPTION} \
      --config_file configs/accelerate/llama.ddp.yaml \
      -m lema.train \
      -c configs/lema/llama8b.lora.yaml \
      "training.run_name='${LEMA_RUN_NAME}.${SKYPILOT_TASK_ID}'" \
      "training.output_dir=/output_dir_gcs/nikolai/runs/${LEMA_RUN_NAME}" \
      "training.per_device_train_batch_size=2" \
      "training.gradient_accumulation_steps=32" \
      "training.max_steps=406" \
      "training.save_steps=0" \
      "training.save_final_model=true"

  echo "Node ${SKYPILOT_NODE_RANK} is all done!"

# train_samples_per_second 59.138 bs==3
# 3*21 = 63 samples per batch per GPU
# 252 samples per batch per node (4 GPU)
# 52000 / 252.0 = 206.34920634920636 steps per 1 epoch (alpaca)

# 2*32 = 64 samples per batch per GPU
# 256 samples per batch per node (4 GPU)
# 52000 / 256 = 203.125 steps per 1 epoch (alpaca)

# 1 epoch (206 steps)
# sky launch -i 10 --env LEMA_RUN_NAME=llama8b.lora.1node.4xA10040GB.206steps.v108 --cloud GCP --use-spot --num-nodes 1 --gpus "A100:4" --cluster xrdaukar-1node4gpu-01-lema-cluster ./configs/skypilot/sky_llama8b_lora.yaml

# 2 epochs (412 steps)
# sky launch -i 10 --env LEMA_RUN_NAME=llama8b.lora.1node.4xA10040GB.412steps.v106 --cloud GCP --use-spot --num-nodes 1 --gpus "A100:4" --cluster xrdaukar-1node4gpu-02-lema-cluster ./configs/skypilot/sky_llama8b_lora.yaml


# bs==2
# 2 epochs (406 steps)
# sky launch -i 10 --env LEMA_RUN_NAME=llama8b.lora.1node.4xA10040GB.406steps.v120 --cloud GCP --use-spot --num-nodes 1 --gpus "A100:4" --cluster xrdaukar-1node4gpu-01-lema-cluster ./configs/skypilot/sky_llama8b_lora.yaml
# https://wandb.ai/lema-train-test/lema-train-test/runs/7228kivr

#                                 total_flos 7.864928994150318e+17
#                                train/epoch 1.99846
#                          train/global_step 406
#                            train/grad_norm 0.12102
#                        train/learning_rate 0.0
#                                 train/loss 0.705
#                train/num_input_tokens_seen 17458224
#       train/telemetry_rank000_epochs_count 2.0
#         train/telemetry_rank000_epochs_max 1036.53809
#        train/telemetry_rank000_epochs_mean 1035.79599
#      train/telemetry_rank000_epochs_median 1035.79599
#         train/telemetry_rank000_epochs_min 1035.05388
#     train/telemetry_rank000_epochs_std_dev 1.04949
#   train/telemetry_rank000_microsteps_count 12929.0
#     train/telemetry_rank000_microsteps_max 0.52468
#    train/telemetry_rank000_microsteps_mean 0.15686
#  train/telemetry_rank000_microsteps_median 0.15221
#     train/telemetry_rank000_microsteps_min 0.00093
# train/telemetry_rank000_microsteps_std_dev 0.02829
#        train/telemetry_rank000_steps_count 405.0
#          train/telemetry_rank000_steps_max 5.49058
#         train/telemetry_rank000_steps_mean 4.68315
#       train/telemetry_rank000_steps_median 4.61442
#          train/telemetry_rank000_steps_min 1.11329
#      train/telemetry_rank000_steps_std_dev 0.44821
#                            train/train_mfu 237.55742
#                       train/train_step_mfu 258.10202
#              train/train_tokens_per_second 2392.373
#                                 train_loss 0.84205
#                              train_runtime 2085.3471
#                   train_samples_per_second 49.841
#                     train_steps_per_second 0.195
#
# https://wandb.ai/lema-train-test/lema-train-test/runs/7228kivr


# bs==2
# 2 epochs (406 steps)
# sky launch -i 10 --env LEMA_RUN_NAME=llama8b.lora.1node.4xA10040GB.406steps.notokparallelism.v121 --cloud GCP --use-spot --num-nodes 1 --gpus "A100:4" --cluster xrdaukar-1node4gpu-01-lema-cluster ./configs/skypilot/sky_llama8b_lora.yaml


# sky launch -i 10 --env LEMA_RUN_NAME=llama8b.lora.1node.4xA10040GB.406steps.notokparallelism.empty_cache.v122 --cloud GCP --use-spot --num-nodes 1 --gpus "A100:4" --cluster xrdaukar-1node4gpu-02-lema-cluster ./configs/skypilot/sky_llama8b_lora.yaml
