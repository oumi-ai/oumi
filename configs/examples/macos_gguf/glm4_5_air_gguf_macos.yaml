# ✅ MACOS COMPATIBLE: LlamaCPP Inference config for GLM-4.5-Air GGUF.
#
# This config uses the LlamaCPP engine which is optimized for CPU inference on macOS.
# LlamaCPP works excellently with GGUF files and doesn't have Triton dependency issues.
#
# Prerequisites:
#   # Install macOS-specific dependencies:
#   pip install oumi[gpu_mac]  # Includes llama-cpp-python with Metal acceleration
#
# Usage:
#   # 1. Download the GGUF file to current working directory:
#   huggingface-cli download unsloth/GLM-4.5-Air-GGUF \
#     GLM-4.5-Air-Q4_K_M.gguf --local-dir .
#
#   # 2. Run inference (CPU optimized):
#   oumi infer -i -c configs/examples/gguf/glm4_5_air_gguf_macos.yaml
#
# Model Info:
#   - Repository: unsloth/GLM-4.5-Air-GGUF
#   - Size: ~9B parameters (Q4_K_M quantized)
#   - File size: ~5.5GB
#   - Requirements: macOS with sufficient RAM (recommend 16GB+ for Q4_K_M)
#   - Performance: CPU-optimized with Metal GPU acceleration if available
#   - Special: Lightweight version of GLM-4 optimized for efficiency
#
# See Also:
#   - Documentation: https://oumi.ai/docs/en/latest/user_guides/infer/infer.html
#   - Config class: oumi.core.configs.InferenceConfig
#   - vLLM GPU version: configs/recipes/glm4/inference/air_gguf_infer.yaml

model:
  # GGUF file path - LlamaCPP uses filename in model_kwargs
  model_name: "unsloth/GLM-4.5-Air-GGUF"
  # Original model tokenizer for proper text processing
  tokenizer_name: "THUDM/glm-4-9b-chat"
  model_max_length: 8192
  torch_dtype_str: "float16"  # GGUF models require float16
  trust_remote_code: True
  # Specify GGUF filename in model_kwargs for LlamaCPP
  model_kwargs:
    filename: "GLM-4.5-Air-UD-Q2_K_XL.gguf"

generation:
  max_new_tokens: 2048
  temperature: 0.7
  top_p: 0.9

engine: LLAMACPP
