# Inference config for GPT-5-chat-latest (always points to the latest GPT-5 chat version)
#
# Requirements:
#   - Set the `OPENAI_API_KEY` env var to your OpenAI API key.
#
# Usage:
#   oumi infer -i -c configs/apis/openai/infer_gpt_5_chat_latest.yaml
#
# See Also:
#   - Documentation: https://oumi.ai/docs/en/latest/user_guides/infer/infer.html
#   - OpenAI API: https://platform.openai.com/docs/models
#   - Config class: oumi.core.configs.InferenceConfig

model:
  model_name: "gpt-5-chat-latest"

engine: OPENAI

generation:
  max_new_tokens: 32768
  temperature: 1.0
