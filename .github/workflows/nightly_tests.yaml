name: Nightly Tests

on:
  schedule:
    - cron: '23 6 * * *' # 6:23 AM UTC ~ 11:23 PM PST
  workflow_dispatch:

jobs:
  cpu-tests:
    secrets: inherit
    uses: ./.github/workflows/pretest.yaml

  gpu-tests:
    secrets: inherit
    uses: ./.github/workflows/gpu_tests.yaml
