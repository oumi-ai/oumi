{% set role_suffix = ': ' %}
{% set turn_suffix = '\n' %}

{% for message in messages %}
    {{ message['role'].upper() + role_suffix + message['content'] | trim + turn_suffix }}
{% endfor %}
{% if add_generation_prompt %}
    {{ "ASSISTANT" + role_suffix }}
{% endif %}
