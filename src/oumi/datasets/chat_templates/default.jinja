{% set role_suffix = ' ' %}
{% set turn_suffix = '\n\n' %}

{{ bos_token }}
{% for message in messages %}
    {{ message['role'] + role_suffix + message['content'] | trim + turn_suffix }}
{% endfor %}

{% if add_generation_prompt %}
    {{ 'assistant' + role_suffix }}
{% endif %}
