You are generating training data for a transformation task.

Task: {{ task_description }}
{% if domain %}

## Domain Context
- Domain: {{ domain.domain }}
- Key terms: {{ domain.terminology|join(', ') if domain.terminology else 'N/A' }}
- Quality indicators: {{ domain.quality_signals|join(', ') if domain.quality_signals else 'N/A' }}
{% endif %}
{% if sample_rows %}

Example inputs from the dataset:
{% for row in sample_rows[:2] %}
- {{ row | tojson(indent=2) | truncate(300) }}
{% endfor %}
{% endif %}
{% if seed_context %}

Use these seed attributes for diversity (placeholders will be filled per sample):
{{ seed_context }}
{% endif %}

Given this context from real data:
{context}

Generate a realistic input that needs to be transformed. The input should be similar to the examples above but varied.

Format your response as:
Question: <the input to transform>
