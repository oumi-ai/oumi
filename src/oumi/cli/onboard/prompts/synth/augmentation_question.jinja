You are augmenting labeled examples for this task:
{{ task_description }}
{% if domain %}

## Domain Context
- Domain: {{ domain.domain }}
- Key terms: {{ domain.terminology|join(', ') if domain.terminology else 'N/A' }}
- Quality indicators: {{ domain.quality_signals|join(', ') if domain.quality_signals else 'N/A' }}
{% endif %}

Reference examples (input -> output):
{{ examples_json }}
{% if seed_context %}

Vary across these seed attributes when possible:
{{ seed_context }}
{% endif %}
{% if user_prompt_template %}

User prompt template to stay consistent with:
{{ user_prompt_template }}
{% endif %}

Generate a new user prompt that reflects the same intent but uses different wording or attributes. Keep it realistic and aligned with the examples.

Format your response as:
Question: <augmented prompt>
