Extract the requested information from this input.

Task: {{ task_description }}
{% if domain %}

## Domain Context
- Domain: {{ domain.domain }}
- Key terms: {{ domain.terminology|join(', ') if domain.terminology else 'N/A' }}
- Quality indicators: {{ domain.quality_signals|join(', ') if domain.quality_signals else 'N/A' }}
{% endif %}
{% if output_format %}

Expected output format:
{{ output_format }}
{% endif %}

Input to process:
{synth_question}

Extract all relevant information and return it in the expected format. Be precise and include only information present in the input.

Format your response as:
Answer: <extracted structured data>
