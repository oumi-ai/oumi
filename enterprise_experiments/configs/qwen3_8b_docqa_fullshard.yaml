# FFT config for Qwen3 4B.
# Some param values are referenced from:
# https://github.com/pytorch/torchtune/blob/main/recipes/configs/qwen3/4B_full.yaml
#
# Requirements:
#   - Log into WandB (`wandb login`) or disable `enable_wandb`
#
# Usage:
#   oumi distributed torchrun -m oumi train -c oumi://configs/recipes/qwen3/sft/4b_full/train.yaml
#
# See Also:
#   - Documentation: https://oumi.ai/docs/en/latest/user_guides/train/train.html
#   - Config class: oumi.core.configs.TrainingConfig
#   - Config source: https://github.com/oumi-ai/oumi/blob/main/src/oumi/core/configs/training_config.py
#   - Other training configs: configs/**/*train.yaml

# CUDA_VISIBLE_DEVICES=0,1,2,3 torchrun --nnodes=1 --node-rank=0 --nproc-per-node=4 --master-addr=127.0.0.1 --master-port=9000 -m oumi train -c "/home/shanghong/oumi/enterprise_experiments/configs/qwen3_8b_docqa_fullshard.yaml" 2>&1 | tee "/home/shanghong/oumi/enterprise_experiments/logs/qwen3_8b_docqa_fullshard.log"

model:
  model_name: "Qwen/Qwen3-8B"
  model_max_length: 2048
  torch_dtype_str: "bfloat16"
  attn_implementation: "sdpa"
  trust_remote_code: True

data:
  train:
    datasets:
      - dataset_name: text_sft_jsonl
        dataset_path: /home/shanghong/oumi/enterprise_experiments/data/docqa_1000_train.json
    collator_name: "text_completions_only_with_padding"
    collator_kwargs:
      response_template: "<|im_start|>assistant\n"
      instruction_template: "<|im_start|>system\n"
    target_col: "messages"
    seed: 42
  validation:
    datasets:
      - dataset_name: text_sft_jsonl
        dataset_path: /home/shanghong/oumi/enterprise_experiments/data/docqa_1000_val.json
    collator_name: "text_completions_only_with_padding"
    collator_kwargs:
      response_template: "<|im_start|>assistant\n"
      instruction_template: "<|im_start|>system\n"
    target_col: "messages"
    seed: 42

training:
  # Basic settings
  trainer_type: "TRL_SFT"
  output_dir: "output/qwen3_8b.fft_docqa_fullshard"
  run_name: "qwen3_8b_docqa_fullshard"

  # Training duration
  num_train_epochs: 3

  # Batch size settings
  per_device_train_batch_size: 1
  per_device_eval_batch_size: 2
  gradient_accumulation_steps: 16
  max_grad_norm: 10

  # Mixed precision and performance
  enable_gradient_checkpointing: True
  gradient_checkpointing_kwargs:
    use_reentrant: False
  compile: False

  # Optimization
  optimizer: "adamw_torch_fused"
  learning_rate: 5e-05
  weight_decay: 0.01

  # Learning rate schedule
  lr_scheduler_type: "cosine"
  warmup_ratio: 0.03

  # Evaluation
  eval_strategy: "steps" # When to evaluate ("no", "steps", "epoch")
  eval_steps: 5 # Evaluate every N steps
  logging_first_step: True

  # Checkpointing
  save_epoch: True
  save_steps: 0

  # Dataloader settings
  dataloader_num_workers: "auto"
  dataloader_prefetch_factor: 32

  # Logging
  logging_steps: 1
  enable_wandb: True
  enable_mlflow: True

  # Performance monitoring
  empty_device_cache_steps: 50
  include_performance_metrics: True

fsdp:
  enable_fsdp: True
  forward_prefetch: True
  cpu_offload: True
  sharding_strategy: "FULL_SHARD"
  auto_wrap_policy: "TRANSFORMER_BASED_WRAP"
  transformer_layer_cls: "Qwen3DecoderLayer"
