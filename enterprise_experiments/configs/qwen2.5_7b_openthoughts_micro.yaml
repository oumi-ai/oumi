model:
  model_name: "Qwen/Qwen2.5-7B-Instruct"
  model_max_length: 16384
  torch_dtype_str: "bfloat16"
  attn_implementation: "sdpa"
  trust_remote_code: True

data:
  train:
    datasets:
      - dataset_name: text_sft_jsonl
        dataset_path: /home/shanghong/oumi/enterprise_experiments/data/open_thoughts_micro_train.json
    collator_name: "text_completions_only_with_padding"
    collator_kwargs:
      response_template: "<|im_start|>assistant\n"
      instruction_template: "<|im_start|>system\n"
    target_col: "messages"
    seed: 42

training:
  # Basic settings
  trainer_type: "TRL_SFT"
  output_dir: "output/qwen2.5_7b.fft_openthoughts_micro"
  run_name: "qwen2.5_7b_openthoughts_micro"

  # Training duration
  num_train_epochs: 3

  # Batch size settings
  per_device_train_batch_size: 1
  per_device_eval_batch_size: 1
  gradient_accumulation_steps: 16
  max_grad_norm: 1

  # Mixed precision and performance
  enable_gradient_checkpointing: True
  gradient_checkpointing_kwargs:
    use_reentrant: False
  compile: False

  # Optimization
  optimizer: "adamw_torch_fused"
  learning_rate: 1e-05
  weight_decay: 0.01

  # Learning rate schedule
  lr_scheduler_type: "cosine"
  warmup_ratio: 0.1

  # Evaluation
  eval_strategy: "steps" # When to evaluate ("no", "steps", "epoch")
  eval_steps: 5 # Evaluate every N steps

  # Checkpointing
  save_epoch: True
  save_steps: 0

  # Dataloader settings
  dataloader_num_workers: "auto"
  dataloader_prefetch_factor: 32
  dataloader_persistent_workers: True

  # Logging
  logging_steps: 1
  enable_wandb: True
  enable_mlflow: True

  # Performance monitoring
  empty_device_cache_steps: 50
  include_performance_metrics: True

fsdp:
  enable_fsdp: True
  forward_prefetch: True
  # cpu_offload: True
  sharding_strategy: "HYBRID_SHARD"
  auto_wrap_policy: "TRANSFORMER_BASED_WRAP"
  transformer_layer_cls: "Qwen2DecoderLayer"
